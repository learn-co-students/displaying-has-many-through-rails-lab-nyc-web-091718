<h1>Doctor Show</h1>

<% @doctor.appointments.each do |appt| %>
  <p><%= link_to appt.patient.name, patient_path(appt.patient) %>'s appointment is on <%= appt.show_date %> <%= appt.show_time %></p>
<% end %>
